				<br><br>
				<h1>Summary</h1>
<table class="table table-striped table-bordered">
	<thead>
		<tr>
			<th>Name</th>
			<th>Actions</th>
		</tr>
	</thead>
	<tbody>
		<% @students.each do |b| %>
		<tr>
			<td><%= b.name %></td>
			<td>
				<%= link_to "Show", student_path(b.id) %>
				<%= link_to "Delete", student_path(b.id), :method => :delete %>
				<%= link_to "Edit", edit_student_path(b.id) %>
			</td>
		</tr>
		<% end %>
	</tbody>
</table>